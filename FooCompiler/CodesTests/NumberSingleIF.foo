startCode {
    int num;
    int fatorial;

    outputKey("Entre com um numero: ");
    inputKey(num);

    if (num > 2) {
        outputKey("O Valor eh maior que 2: ");
        outputKey(num);
    };
    outputKey("Fim do programa");
}endCode